dsl konos

Service(title = "Quassar Artifactory") quassar-artifactory-api as REST(host = "{host}", basePath = "/releases", port = "{port}")

	Resource("*") artifactoryFile
    	Get(description = "Download release file")
    		Response as File
    		Exception(Forbidden)
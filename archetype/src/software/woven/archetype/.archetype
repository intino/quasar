TARGETS:{editor}

+ configuration
    - modelSequence() in "model-sequence.tsv"

- languages in "languages.tsv"

+ models in "models"
    - definition(name) in "{name}/definition.json"
    - workspace(name, version) in "{name}/workspaces/{version}"

+ tmp

+ repository
	+ languages